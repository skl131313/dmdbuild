{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "options": {
                "env": {
                    "ROOT": "${workspaceRoot}"
                },
                "shell": {
                    "executable": "cmd.exe",
                    "args": [ "/c" ],

                }
            },
            "command": ".\\workaround.cmd",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
